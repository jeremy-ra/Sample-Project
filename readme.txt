To run the app, set the database first:
1. Run the sql script inside the solution folder named CreateDB_Table.sql to create the Database and Table.

2. Update the connection string(point to the local DB created in step 1) in the OnConfiguring method in Model folder > TestDBContext.cs class.

